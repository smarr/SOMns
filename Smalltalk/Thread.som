Thread = (
    name = primitive
    name: aString = primitive
    join = primitive
    
    ----
    new = ( ^ self error: 'Use Block>>#spawn* to create a thread.' )
    
    yield   = primitive
    
    "Current may return `nil` for the main thread."
    current = primitive
)

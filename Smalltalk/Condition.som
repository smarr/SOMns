Condition = (
    "A condition variable to be used together with a Mutex"
    
    signalOne = primitive
    signalAll = primitive

    await = primitive
    await: timeoutMilliseconds = primitive
    
    ----
    
    new = ( self error: 'Condition variables are created from mutexes.' )
)
